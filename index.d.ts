/** Given an IP, will return if the IP is from Roblox or not. */
declare const is_roblox_ip: {
  /** Given an IP, will return if the IP is from Roblox or not. */
  (ip: string): boolean
  /** Given an IP, will return if the IP is from Roblox or not. */
  is_roblox_ip (ip: string): boolean
}

/** Given an IP, will return if the IP is from Roblox or not. */
export = is_roblox_ip
